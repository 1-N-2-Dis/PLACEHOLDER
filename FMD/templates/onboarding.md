# Onboarding — <project>

> **Purpose:** get a new contributor OR a fresh AI agent productive in the first few minutes:
> what this is, how to run it, and where to look. Kept to one screen. It **points**, it does not
> restate — every fact lives in its canonical doc (see `index.md` §0).

## What this is
<!-- One or two lines. The elevator version. Link to idea.md / north-star. -->

## Read first
<!-- The 2–3 docs that orient everything. Always start with index.md §0 (source-of-truth map). -->
- `docs/index.md` §0 — which doc owns which fact (read this first)
- `docs/prd.md` — what we're building
- `docs/system-design.md` — how it's built
- `docs/implementation-plan.md` — live `TASK-###` work, owners, dependencies, scopes, gates, and the optional GitHub Issue/Project projection *(if generated)*

## Run it locally
<!-- Setup in numbered steps. Pinned versions. The one command that proves it works. -->
1. <prereqs / versions>
2. <install>
3. <run>
4. <verify — the command/URL that confirms it's working>

## Where things live
<!-- Map the repo: app code, docs, tests, scripts. Point to the SAD if there's a build crew. -->

## Conventions
<!-- Keep only what applies. If a living plan exists: claim one ready TASK-###, use
`task/TASK-###-short-slug`, stay inside its write scope, open a small PR, attach exact gate evidence,
and checkpoint the plan/canonical docs before ending the session. No direct shared-branch work. -->
