# RFC — <short title> (<project>)

> **Purpose:** a lightweight, per-feature design/decision note. Use it when a single feature has a
> non-obvious design choice worth recording, so the *why* survives past the moment. Not heavy
> process — CJ's Axon-Enjin builds carry `rfc-<proj>-<feature>.md` even in fast builds.
> Traces to: the `F-###` feature(s) it concerns. Retires by folding its decision into the PRD/ADR.

## Feature / scope
<!-- The F-### this belongs to, and exactly what's in question. One feature, one decision. -->

## Context
<!-- Why this needs a decision. Constraints, the forces at play. -->

## Proposal
<!-- The recommended approach. Concrete. -->

## Alternatives considered
<!-- 1–3 options and why each loses. Include "do the simplest thing" as a baseline. -->

## Decision & status
<!-- Chosen option + status: Open | Decided | Superseded. When Decided, note where it folds
     (PRD section / ADR id). -->

## Open questions
<!-- Anything unresolved. Mark [assumption] if you proceeded without an answer. -->
